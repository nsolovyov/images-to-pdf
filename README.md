Simply converts a list of images to the PDF file

1. `yarn install`.
1. Put images into the `images/` folder.
1. Run `yarn start`
1. Take your PDF file from the `pdf/` folder. The filename is generation time.
